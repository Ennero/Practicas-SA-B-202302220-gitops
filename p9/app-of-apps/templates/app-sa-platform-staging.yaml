{{- if .Values.applications.platformStaging.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.applications.platformStaging.name }}
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: sa-platform-root
    environment: staging
    course: sa-b-2026
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: {{ .Values.spec.source.targetRevision }}
    path: {{ .Values.applications.platformStaging.path }}
    helm:
      valueFiles:
        {{- range .Values.applications.platformStaging.valuesFiles }}
        - {{ . }}
        {{- end }}
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: {{ .Values.applications.platformStaging.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
{{- end }}
